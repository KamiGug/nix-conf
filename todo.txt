ssh module
sshd module
make waybar more usable
use noctalia-shell
trim system wide plasma
add plasma hm config
migrate waybar to templating rather than function
migrate tmux to templating rather than inline
migrate lazygit to templating rather than inline
something about git and ssh
add mako (notification daemon)
.dev (bashly based project management) / taskfile + lefthook
