migrate to tnix

SOPS for:
    - ssh (hm)
    - sshd (nixos)
    - app services (nixos)
    - nfs pass(?) (nixos)

arbitrary DE choice with autologin (?)
add file browser/manager/explorer to niri (pkg and a shortcut)

add nfs server + client
add remote desktop (connect to current) as a systemd unit

write scripts:
  save-niri-workspaces:
    - takes in workspace ids (--workspaces/-w <comma_separated_ids>) - saves to niri.json at current cwd (+ --out/-o <dir>)

  open niri workspaces:
    - takes in a path to niri.json (positional; defaults to cwd/niri.json)
    - needs to ask each kitty os window/tab/window for cwd
    - if a zed was open - need to open the same number of windows (or maybe just one for each git root in the saved dirs)


move gameify to an hm module
ssh module
sshd module
EmulationStation DE
trim system wide plasma
add plasma hm config
migrate tmux to templating rather than inline
migrate lazygit to templating rather than inline
templating for git and ssh
add mako (notification daemon) (niri manual)
make waybar more usable (niri manual)

add automatic pkg adding
add mkHost function
