arbitrary DE choice
add file browser/manager/explorer to niri (pkg and a shortcut)

add nfs server + client
add remote desktop (connect to current) as a systemd unit

move gameify to an hm module
ssh module
sshd module
EmulationStation DE
trim system wide plasma
add plasma hm config
migrate tmux to templating rather than inline
migrate lazygit to templating rather than inline
templating for git and ssh
add mako (notification daemon) (niri manual)
make waybar more usable (niri manual)
.dev (bashly based project management) / taskfile + lefthook

add automatic pkg adding
add mkHost function
